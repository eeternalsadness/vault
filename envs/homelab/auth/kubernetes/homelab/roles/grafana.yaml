metadata:
  name: grafana
spec:
  tokenPolicies:
    - default
    - grafana-read
  tokenTtlSeconds: 3600 # 1 hour
  serviceAccountNames:
    - kube-prometheus-grafana
  serviceAccountNamespaces:
    - monitoring
