metadata:
  name: cert-manager
spec:
  tokenPolicies:
    - default
    - cert-manager
  tokenTtlSeconds: 3600 # 1 hour
  serviceAccountNames:
    - external-secrets
  serviceAccountNamespaces:
    - external-secrets
